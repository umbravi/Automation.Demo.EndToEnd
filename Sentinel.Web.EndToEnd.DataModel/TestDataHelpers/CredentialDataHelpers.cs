namespace Sentinel.Web.EndToEnd.DataModel.TestDataHelpers
{
    public static partial class DataHelpers
    {
        public static (string userEmail, string userPassword) GetUserCredential()
        {
            var userEmail = "";
            var userPassword = "";

            /// Will implement mock user credential pull and return the credential

            return (userEmail, userPassword);
        }
    }
}
